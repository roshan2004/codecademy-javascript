//Creating Object Literals
// An object literal with two key-value pairs

let spaceship = {
    'Fuel Type': 'diesel', //Fuel type has quotation because it contains space
    color: 'silver'
};

/* The spaceship object has two properties,
We've filled the object with unordered data which is organized into key
-value pairs. A key is like a variable name 
that points to a location in memory that holds a value
 */
// Write your fasterShip object literal
let fasterShip = {
    'Fuel Type': 'Turbo Fuel',
    color : 'silver'
};
